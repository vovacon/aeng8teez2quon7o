# Конфигурация исключений систем безопасности

## Общая информация

В проекте настроены исключения для систем безопасности и сканирования, чтобы избежать false-positive срабатываний.

## Директории и файлы для исключения

### 📁 **Директория `+/`**
- **Назначение**: Вспомогательные скрипты, утилиты разработки, тестовые данные
- **Проблема**: Содержит mock данные, User-Agent строки, примеры API ключей
- **Решение**: Полное исключение из всех сканеров

### 🧪 **Тестовые файлы**
- **Паттерны**: `test_*.rb`, `*_test.py`, `*.tmp`, `*.bak`
- **Проблема**: Могут содержать тестовые токены и ключи
- **Решение**: Исключение по паттернам файлов

### 🔧 **Системные директории**
- **Директории**: `.git/`, `vendor/`, `tmp/`, `log/`, `utils/`
- **Проблема**: Могут содержать логи с чувствительными данными
- **Решение**: Общие исключения для всех сканеров

## Конфигурационные файлы

| Файл | Назначение | Инструмент |
|------|------------|----------|
| `.gitleaks.toml` | Поиск секретов в Git | GitLeaks |
| `.secretsignore` | Общие сканеры секретов | Universal |
| `.semgrepignore` | Статический анализ | Semgrep |
| `.trivyignore` | Уязвимости и секреты | Trivy |
| `.scanignore` | Общие сканеры | Universal |
| `.security-ignore` | Специализированные | Custom |

## Известные false-positive паттерны

### User-Agent строки
```ruby
@user_agent = "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36"
```
**Проблема**: Может быть ошибочно определена как чувствительные данные

### Тестовые ключи
```
example_api_key
test_token  
demo_secret
```
**Проблема**: Примеры в документации могут быть приняты за реальные

### localhost адреса
```
localhost:3000
127.0.0.1:6379  
0.0.0.0:8080
```
**Проблема**: Локальные адреса для разработки не представляют угрозы

## Использование

### Локальные тесты
```bash
# Запуск всех тестов
chmod +x run_all_tests.sh
./run_all_tests.sh

# Проверка GitLeaks
gitleaks detect --config .gitleaks.toml

# Проверка Semgrep
semgrep --config=p/security-audit .
```

### CI/CD
Конфигурация в `.github/workflows/quality-checks.yml` автоматически использует все исключения.

## Поддержка

При добавлении новых тестовых файлов или скриптов, обновите:
1. Соответствующие ignore-файлы
2. CI/CD конфигурацию при необходимости
3. Данную документацию
