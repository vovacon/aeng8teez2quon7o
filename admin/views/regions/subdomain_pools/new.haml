- # encoding: utf-8
%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:'regions/subdomain_pools', :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:'regions/subdomain_pools', :new)

.tabs-content
  -form_for :subdomain_pool, url(:'regions/subdomain_pools', :create), :class => 'form-horizontal' do |f|
    =partial 'regions/subdomain_pools/form', :locals => { :f => f }
