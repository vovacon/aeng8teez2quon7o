- # encoding: utf-8
=partial 'seo/tabs', :locals => {:entity => :scripts, :action => :new}

.tabs-content
  -form_for :script, url(:'seo/scripts', :create), multipart: true, :class => 'form-horizontal' do |f|
    =partial 'seo/scripts/form', :locals => { :f => f }
