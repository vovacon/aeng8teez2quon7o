name: 🔒 Advanced Security Scanning

on:
  push:
    branches: [ main, sketch-wip ]
    paths:
      - 'app/**'
      - 'admin/**'
      - 'config/**'
      - 'lib/**'
      - 'Gemfile*'
      - '.gitleaks.toml'
      - '.github/workflows/security-advanced.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'app/**'
      - 'admin/**'
      - 'config/**'
      - 'lib/**'
      - 'Gemfile*'
  workflow_dispatch:
    inputs:
      scan_type:
        description: 'Тип сканирования'
        required: false
        default: 'all'
        type: choice
        options:
        - all
        - gitleaks
        - semgrep
        - trivy

jobs:

  advanced-security-scan:
    name: 🔒 Продвинутое сканирование безопасности
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
        
    - name: Run GitLeaks (scan all files)
      run: |
        # Download and run GitLeaks directly to avoid git revision issues
        echo "🔍 Запуск GitLeaks для поиска секретов..."
        
        # Use simple filesystem scan instead of git diff scan
        wget -q https://github.com/gitleaks/gitleaks/releases/download/v8.24.3/gitleaks_8.24.3_linux_x64.tar.gz
        tar -xzf gitleaks_8.24.3_linux_x64.tar.gz
        
        # Run filesystem scan with our config
        ./gitleaks detect --source . --config .gitleaks.toml --verbose --no-git || {
          echo "⚠️  GitLeaks обнаружил потенциальные секреты"
          echo "ℹ️  Проверьте логи выше - могут быть false positives"
        }
        
        echo "✅ GitLeaks сканирование завершено"
      continue-on-error: true
      
    - name: Check GitLeaks results
      if: always()
      run: |
        if [ -f "results.sarif" ]; then
          echo "✅ GitLeaks scan completed with results"
        else
          echo "⚠️  GitLeaks scan completed without SARIF output"
        fi
        
    - name: Run Semgrep
      uses: semgrep/semgrep-action@v1
      with:
        config: >-
          p/security-audit
          p/secrets
          p/owasp-top-ten
      env:
        SEMGREP_RULES: p/security-audit
        SEMGREP_BASELINE_REF: main
        # Respect .semgrepignore
      continue-on-error: true
        
    - name: Show files that will be scanned by Trivy
      run: |
        echo "📁 Files and directories that will be scanned:"
        find . -type f \( -name "*.rb" -o -name "*.yml" -o -name "*.yaml" -o -name "*.gemfile" -o -name "Gemfile*" \) \
          ! -path "./+/*" ! -path "./.git/*" ! -path "./vendor/*" ! -path "./tmp/*" \
          | head -10
        echo "..."
        
    - name: Run Trivy filesystem scan
      uses: aquasecurity/trivy-action@master
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
        severity: 'CRITICAL,HIGH,MEDIUM'
        exit-code: '0'
      continue-on-error: true
        
    - name: Check if SARIF file exists
      id: sarif-check
      run: |
        if [ -f "trivy-results.sarif" ] && [ -s "trivy-results.sarif" ]; then
          echo "sarif_exists=true" >> $GITHUB_OUTPUT
          echo "✅ SARIF file created successfully"
        else
          echo "sarif_exists=false" >> $GITHUB_OUTPUT
          echo "⚠️  SARIF file not created or empty"
        fi
        
    - name: Upload Trivy scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v3
      if: steps.sarif-check.outputs.sarif_exists == 'true'
      with:
        sarif_file: 'trivy-results.sarif'
      continue-on-error: true

  vulnerability-scanning:
    name: 🔍 Поиск уязвимостей
    runs-on: ubuntu-latest
    needs: [advanced-security-scan]
    if: always() && (github.event.inputs.scan_type == 'trivy' || github.event.inputs.scan_type == 'all' || github.event.inputs.scan_type == '')
    
    steps:
    - uses: actions/checkout@v4
      
    - name: 🔍 Поиск уязвимостей в зависимостях
      run: |
        echo "🔍 Проверка зависимостей на уязвимости..."
        
        # Проверяем критичные гемы на уязвимости
        CRITICAL_GEMS=("activerecord" "mysql2" "padrino" "sidekiq" "redis")
        
        echo "📊 Критичные гемы для мониторинга:"
        for gem in "${CRITICAL_GEMS[@]}"; do
          if grep -q "gem ['\"]$gem['\"]" Gemfile; then
            version=$(grep "gem ['\"]$gem['\"]" Gemfile | sed "s/.*['\"]$gem['\"][[:space:]]*,[[:space:]]*['\"]//' | sed "s/['\"].*//")
            echo "  ℹ️  $gem: $version"
          fi
        done
        
        echo "ℹ️  Для полной проверки уязвимостей см. Trivy scan выше"
        
  summary:
    name: 📊 Сводка результатов
    runs-on: ubuntu-latest
    needs: [advanced-security-scan, vulnerability-scanning]
    if: always()
    
    steps:
    - name: 📊 Общая сводка
      run: |
        echo "## 🔒 Результаты продвинутого сканирования безопасности"
        echo ""
        echo "### ✅ Успешно:"
        
        if [[ "${{ needs.advanced-security-scan.result }}" == "success" ]]; then
          echo "- 🔍 GitLeaks: Проверка секретов в коде"
          echo "- 🔍 Semgrep: Static Application Security Testing"
          echo "- 🔍 Trivy: Поиск уязвимостей"
        fi
        
        if [[ "${{ needs.vulnerability-scanning.result }}" == "success" ]]; then
          echo "- 📊 Мониторинг зависимостей"
        fi
        
        echo ""
        echo "### 🔗 Интеграция:"
        echo "- ℹ️  Основные тесты и качество кода: '.github/workflows/tests.yml'"
        echo "- ℹ️  Продвинутое сканирование: '.github/workflows/security-advanced.yml'"
