-# Простейшая отладка без helper'ов
.simple-debug{ style: "position: fixed; top: 10px; right: 10px; background: #ff0000; color: white; padding: 10px; font-size: 14px; z-index: 99999; border: 2px solid #000; font-weight: bold;" }
  [!] DEBUG PANEL LOADED
  %br
  Layout: LOADED
  %br
  Time: #{Time.now.strftime('%H:%M:%S')}

-# Простая панель корзины без условий
.simple-cart-panel{ style: "position: fixed; bottom: 0; left: 0; right: 0; height: 48px; background: #2ecc71; color: white; display: flex; align-items: center; justify-content: center; z-index: 9999; font-weight: bold;" }
  [CART] ПАНЕЛЬ КОРЗИНЫ - ТЕСТ

:javascript
  console.log('Simple debug panel loaded at', new Date());
  console.log('Screen width:', window.innerWidth);
