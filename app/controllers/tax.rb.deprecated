# encoding: utf-8
# DEPRECATED: Этот файл устарел и заменён методом в app/models/order.rb
# Переименован чтобы избежать переопределения исправленного метода parse_price

# Если этот функционал всё ещё нужен, перенесите его в отдельный класс
# и не переопределяйте методы модели Order

# class OrderTaxCalculator
#   def self.parse_price(key)
#     # Здесь может быть логика, если нужна
#   end
# end
