# Система управления правами администраторов

Система позволяет гибко управлять доступом администраторов к различным разделам админ-панели.

## 🚀 Установка

### 1. Обновление БД

Выполните SQL запрос из файла `db/add_permissions_field.sql`:

```sql
ALTER TABLE accounts ADD COLUMN role_permissions TEXT DEFAULT NULL;
UPDATE accounts SET role_permissions = '[]' WHERE role_permissions IS NULL;
```

### 2. Перезапуск приложения

Пеrezапустите приложение для применения изменений в коде.

## 👥 Роли пользователей

- **admin** - полные права доступа ко всем разделам
- **manager** - доступ к указанным модулям + право управления клиентами
- **editor** - доступ только к указанным модулям контента

## 🎛 Доступные модули

| Модуль | Описание |
|--------|----------|
| `products` | Товары |
| `categories` | Категории |
| `orders` | Заказы |
| `comments` | Комментарии |
| `news` | Новости |
| `articles` | Статьи |
| `pages` | Страницы |
| `clients` | Клиенты |
| `contacts` | Контакты |
| `seo` | SEO |
| `payment` | Платежи |
| `regions` | Регионы |
| `delivery` | Доставка |
| `discounts` | Скидки |
| `smiles` | Отзывы (Смайлы) |
| `accounts` | Учетные записи |

## 🔧 Использование

### Доступ к управлению правами

1. Войдите в админку как `admin`
2. Перейдите в раздел **"Управление правами"** `/admin/permissions`
3. Выберите пользователя для редактирования прав

### Управление правами

#### Индивидуальное редактирование:
- Откройте страницу редактирования пользователя
- Выберите модули, к которым нужен доступ
- Сохраните изменения

#### Массовое редактирование:
- Выберите несколько пользователей на главной странице
- Нажмите "Массовое редактирование"
- Выберите права и примените

### API для проверки прав

```ruby
# В контроллерах
can_access_module?('products')
can_edit_module?('categories')
can_delete_module?('orders')

# В моделях
account.has_permission?('products')
account.permissions # => ['products', 'categories']
```

## 🔐 Безопасность

- **Администраторы** имеют полные права независимо от настроек
- **Только админы** могут управлять правами других пользователей
- Самоудаление админов запрещено
- Проверка прав происходит на уровне маршрутов

## 🧪 Тестирование

Запустите тестовый скрипт для проверки работоспособности:

```bash
./test_permissions_system.rb
```

## 📱 Интерфейс

### Главная страница админки
- Показывает доступные пользователю модули
- Кнопка "Настроить права" (только для админов)

### Страница управления правами
- Список всех пользователей с их правами
- Массовое редактирование
- Детальный просмотр прав

### Страница редактирования прав
- Удобная сетка модулей с чекбоксами
- Быстрые кнопки: "Выбрать все", "Очистить", "Обычные модули"

## ⚙️ Техническая информация

### Файлы системы:

```
models/account.rb                    # Обновленная модель
admin/controllers/permissions.rb     # Контроллер управления
admin/views/permissions/            # Интерфейс управления
admin/helpers/permissions_helper.rb # Вспомогательные методы
admin/helpers/application_helper.rb # Проверки доступа
db/add_permissions_field.sql       # SQL для БД
test_permissions_system.rb         # Тесты
```

### Структура permissions в БД:

```json
// В поле role_permissions хранится JSON массив:
["products", "categories", "orders"]
```

### Методы модели Account:

- `permissions` - получить список разрешений
- `permissions=` - установить разрешения
- `has_permission?(module)` - проверить разрешение
- `add_permission(module)` - добавить разрешение
- `remove_permission(module)` - удалить разрешение
- `display_name` - отображаемое имя

---

*Система готова к использованию после выполнения SQL запроса и перезапуска приложения.*
