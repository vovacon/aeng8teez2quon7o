# Исправление интеграции Smile с заказами

## Описание проблемы

В коде были обнаружены две критические ошибки:

### 1. Неправильные SQL-запросы
Использовался неправильный ключ связи в нескольких местах:

### 2. Переменная %main_product_name% не работала
Логика в `seo_helper.rb` работала только с старыми данными `json_order`, не учитывая новую логику `products_data`.

**Первоначально предполагали:**
```sql
SELECT * FROM order_products WHERE order_id = <order.id>
```

**Однако реальная структура БД:**
```sql
SELECT * FROM order_products WHERE id = <order.id>
```
*Поле `id` в таблице `order_products` является внешним ключом на `orders.id`*

## Затронутые файлы

### 1. `app/models/smile.rb`
**Метод:** `order_products_for_display`
- **Строка 135:** Исправлен SQL-запрос для получения товаров заказа
- **Функциональность:** Позволяет отображать актуальные товары заказа на страницах smiles вместо устаревших данных из json_order

### 2. `app/controllers/payment.rb`  
**Контекст:** Получение корзины для формирования чека
- **Исправлено:** SQL-запрос для получения товаров заказа при оплате

### 3. `app/controllers/api.rb`
**Контекст:** API для экспорта заказов
- **Исправлено:** SQL-запрос для получения товаров заказа в API

### 4. `admin/controllers/smiles.rb`
**Контекст:** API endpoint `/order_products/:order_id` в админке
- **Исправлено:** SQL-запрос для получения товаров заказа в админке
- **Функциональность:** Позволяет корректно отображать товары заказа в админке

### 5. `app/helpers/seo_helper.rb`
**Метод:** Обработка переменной `%main_product_name%`
- **Обновлено:** Логика теперь использует `@smile.products_data` вместо напрямую `json_order`
- **Функциональность:** Переменная `%main_product_name%` теперь работает с данными из реального заказа

### 6. `app/app.rb` (закомментированный код)
**Контекст:** Тестовый endpoint
- **Исправлено:** SQL-запрос для консистентности

## Логика работы

После исправлений система работает следующим образом:

1. **Приоритет данных на страницах smiles:**
   - Если у отзыва есть `order_eight_digit_id` → используются актуальные товары из таблицы `order_products`
   - Если нет → fallback к старым данным `json_order`
   - Если и это не работает → пустой результат

2. **Преимущества нового подхода:**
   - Актуальные названия товаров (даже если товар изменился после заказа)
   - Корректные цены на момент заказа
   - Информация о количестве товаров
   - Возможность отследить связь с конкретным товаром

## Структура данных

### Таблица `orders`
- `id` (PK) - внутренний ID заказа
- `eight_digit_id` - публичный номер заказа (8 цифр)

### Таблица `order_products`
- `base_id` (PK, AUTO_INCREMENT) - внутренний ID записи
- `id` (FK) - ссылка на `orders.id` **(важно!)**
- `product_id` - ID товара
- `title` - название товара на момент заказа
- `price` - цена на момент заказа
- `quantity` - количество
- `typing` - тип комплекта (standard, premium, etc.)
- `date_from`, `date_to` - даты доставки

**Особенность:** Поле `id` является внешним ключом, а `base_id` - primary key!

### Таблица `smiles`
- `order_eight_digit_id` - связь с `orders.eight_digit_id`
- `json_order` - старые данные (fallback)

## Тестирование

### Автоматические тесты
- **Unit тест:** `tests/unit/smile_products_data_test.rb` - проверяет логику приоритетов
- **Интеграционный тест:** `test_smile_logic.rb` - проверяет работу с mock-данными

### Ручное тестирование
1. Открыть страницу `/smiles/<slug>` для отзыва с привязанным заказом
2. Проверить, что отображаются корректные товары и цены
3. Сравнить с данными в админке заказа

## Совместимость

- ✅ **Обратная совместимость:** Старые отзывы без `order_eight_digit_id` продолжают работать через `json_order`
- ✅ **Производительность:** Минимальное влияние на производительность
- ✅ **Безопасность:** Использование параметризованных запросов предотвращает SQL-инъекции

## Дата внесения изменений
**25 октября 2025**

## Интеграция с комментариями клиентов (v2.1)

### Обзор

Добавлена функциональность отображения связанных комментариев клиентов на страницах smiles (`/smiles/<slug>`). Если у smile есть `order_eight_digit_id` и существует опубликованный комментарий для этого заказа, он будет отображаться на странице.

### Связь данных

```
smiles.order_eight_digit_id → orders.eight_digit_id → comments.order_eight_digit_id
```

### Реализация

#### 1. Модель Smile (app/models/smile.rb)

**Обновленные методы:**

```ruby
# Получение связанного комментария (только опубликованные)
def related_comment
  return nil unless order_eight_digit_id.present?
  
  begin
    if defined?(Comment)
      Comment.published.find_by_order_eight_digit_id(order_eight_digit_id)
    end
  rescue => e
    Rails.logger.error "Error fetching comment for smile #{id}: #{e.message}" if defined?(Rails)
    nil
  end
end

# Проверка наличия комментария для отображения
def has_review_comment?
  comment = related_comment
  comment && comment.body.present?
end
```

#### 2. Шаблон страницы (app/views/smiles/show.erb)

Добавлен блок отображения комментария после таблицы с товарами:

```erb
<% if @post.has_review_comment? %>
  <%
    review_comment = @post.related_comment
    comment_author = review_comment.name.present? ? review_comment.name : (@post.customer_name || 'Клиент')
    comment_date = review_comment.date || review_comment.created_at
    comment_rating = review_comment.rating || @post.rating || 5
  %>
  <div style="margin: 32px 0; padding: 24px; background: #f9f9f9; border-left: 4px solid #4CAF50; border-radius: 4px;">
    <h3>💬 Отзыв клиента</h3>
    <!-- Отображение автора, даты, рейтинга и текста -->
  </div>
<% end %>
```

#### 3. Тестирование

**Добавлены тесты:**
- `test_smile_related_comment_returns_nil_when_no_order_id` - проверка отсутствия комментария без order_id
- `test_smile_has_review_comment_detection` - проверка определения наличия комментария
- `test_smile_has_no_review_comment_when_body_empty` - проверка поведения с пустым комментарием
- `test_smile_has_no_review_comment_when_no_comment` - проверка отсутствия комментария

**Общее количество тестов:** 47 тестов, 268 assertions

### Отображаемые элементы

1. **Заголовок блока:** "💬 Отзыв клиента"
2. **Автор:** Имя из комментария или customer_name из smile
3. **Дата:** Дата комментария в формате dd.mm.yyyy
4. **Рейтинг:** Звездочки (используется существующий partial `_rating`)
5. **Заголовок:** Заголовок комментария (если указан)
6. **Текст:** Основной текст отзыва с переносами строк
7. **Номер заказа:** Отображается внизу блока

### Стилизация

- Фоновый цвет: `#f9f9f9`
- Левая граница: `4px solid #4CAF50` (зеленый акцент)
- Закругленные углы: `border-radius: 4px`
- Отступы: `margin: 32px 0; padding: 24px`
- Адаптивный дизайн с `flex-wrap` для мобильных устройств

### Безопасность

- Весь пользовательский контент экранируется через `h()`
- Отображаются только **опубликованные** комментарии (`Comment.published`)
- Защита от XSS с помощью `html_safe` только для безопасных операций

### Обратная совместимость

- ✅ Не влияет на существующие smile без комментариев
- ✅ Микроразметка schema.org Review продолжает работать
- ✅ Все существующие функции сохранены
- ✅ Graceful degradation при отсутствии модели Comment

### Примеры использования

**Smile с комментарием:**
```
/smiles/12345 → отображает товары + блок с отзывом клиента
```

**Smile без комментария:**
```
/smiles/67890 → отображает только товары (как раньше)
```

### Логирование и отладка

Ошибки загрузки комментариев логируются через `Rails.logger.error` (если доступно Rails).
