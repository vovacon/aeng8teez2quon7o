# Мобильная панель корзины

## Описание
Мобильная панель корзины — это фиксированная панель внизу экрана, которая отображается на всех страницах сайта (кроме страниц корзины и оформления заказа) при наличии товаров в корзине.

## Основные характеристики

### ✅ Реализованные требования:
- **Высота**: 48px (как требовалось)
- **Позиционирование**: Fixed bottom (прикреплена к нижней части экрана)
- **Отображение**: Только на мобильных устройствах (≤768px)
- **Видимость**: Только при наличии товаров в корзине (≥1 товар)
- **Исключения**: Скрыта на страницах `/cart/*`

### 🎨 Элементы панели:
1. **Счетчик товаров** — круглый красный badge с количеством товаров
2. **Кнопка "В корзину"** — зеленая кнопка для перехода в `/cart`
3. **Сумма корзины** — общая стоимость с символом ₽

## Файловая структура

```
app/
├── helpers/
│   └── cart_helper.rb                    # Расширенные методы для работы с корзиной
└── views/
    └── layouts/
        ├── application.haml              # Подключена панель
        ├── catalog.haml                  # Подключена панель  
        └── parts/
            ├── _mobile_cart_panel.haml   # HTML и CSS панели
            └── _mobile_cart_panel_js.haml # JavaScript функциональность
```

## Технические детали

### Helper методы (cart_helper.rb)
```ruby
# Подсчет общего количества товаров в корзине
def cart_items_count
  return 0 if session[:cart].nil? || session[:cart].empty?
  session[:cart].sum { |item| item["quantity"].to_i }
end

# Проверка наличия товаров в корзине
def cart_has_items?
  !session[:cart].nil? && !session[:cart].empty?
end

# Проверка, нужно ли показывать панель
def should_show_cart_panel?
  return false unless cart_has_items?
  
  current_path = request.path_info
  excluded_paths = ['/cart', '/cart/', '/cart/show', ...]
  
  return false if excluded_paths.any? { |path| current_path.start_with?(path) }
  true
end
```

### API endpoint для обновления данных
Панель использует существующий endpoint `/cart/stat` который возвращает:
```json
{
  "total_s": 2890,  // общая сумма в рублях
  "total_q": 3      // общее количество товаров
}
```

### CSS стили
- **Responsive**: Показывается только на экранах ≤768px
- **Z-index**: 9999 для отображения поверх всего контента
- **Box-shadow**: Тень сверху для визуального отделения
- **Flexbox**: Равномерное распределение элементов
- **Padding-bottom**: Автоматически добавляется к body (48px)

### JavaScript функциональность
- **Auto-update**: Автоматическое обновление при загрузке страницы
- **AJAX**: Периодическое обновление данных корзины
- **Event listeners**: Реагирует на добавление товаров в корзину
- **Resize handler**: Адаптируется при изменении размера окна
- **Error handling**: Обработка ошибок AJAX запросов

## Использование

### Подключение в layout
Панель автоматически подключается в основных layout'ах:

```haml
-# В конце файла перед закрытием body
= partial 'layouts/parts/mobile_cart_panel'
```

### Кастомизация
Для изменения внешнего вида отредактируйте CSS в файле `_mobile_cart_panel.haml`:

```css
.mobile-cart-panel {
  height: 48px;                    /* Высота панели */
  background-color: #fff;          /* Фон панели */
  border-top: 1px solid #ddd;      /* Верхняя граница */
}

.mobile-cart-panel__count-badge {
  background-color: #ff4757;       /* Цвет badge счетчика */
}

.mobile-cart-panel__cart-button {
  background-color: #2ecc71;       /* Цвет кнопки */
}
```

### События JavaScript
Можно слушать кастомные события:

```javascript
// Обновление корзины
document.addEventListener('cart:updated', function() {
  console.log('Корзина обновлена');
});

// Ручное обновление панели
if (typeof updateMobileCartPanel === 'function') {
  updateMobileCartPanel();
}
```

## Тестирование

### Демо-страница
Создана демо-страница `demo_mobile_cart_panel.html` для тестирования:
- Визуальная демонстрация панели
- Кнопки для тестирования разных состояний
- Адаптивный дизайн

### Тестовые сценарии
1. **Пустая корзина** — панель не отображается
2. **1+ товаров** — панель появляется
3. **Страницы корзины** — панель скрыта
4. **Мобильные устройства** — панель видна
5. **Десктоп** — панель скрыта
6. **Обновление данных** — корректное отображение изменений

## Производительность

- **Lazy loading**: Панель загружается только при необходимости
- **Throttled updates**: Ограничение частоты AJAX запросов  
- **Efficient selectors**: Оптимизированные CSS и JS селекторы
- **Memory cleanup**: Правильная очистка event listener'ов

## Совместимость

- ✅ **Ruby**: Совместимо с Padrino framework
- ✅ **Browsers**: Современные браузеры (поддержка Flexbox)
- ✅ **Mobile**: iOS Safari, Android Chrome
- ✅ **Responsive**: Bootstrap-совместимые breakpoints

## Troubleshooting

### Панель не отображается
1. Проверьте наличие товаров в `session[:cart]`
2. Убедитесь что это не страница корзины
3. Проверьте ширину экрана (должна быть ≤768px)

### Неверные данные
1. Проверьте работу endpoint `/cart/stat`
2. Проверьте метод `total` в cart_helper
3. Проверьте JavaScript консоль на ошибки

### Проблемы со стилями
1. Убедитесь что CSS не конфликтует с другими стилями
2. Проверьте z-index других элементов
3. Проверьте viewport мета-тег

---

**Автор**: Sketch AI Assistant  
**Дата**: 2024-11-12  
**Версия**: 1.0
